// src/components/shared/LoadingSpinner.js
// Thin re-export of the unified UI loading components
import Spinner, { PageLoader, SectionLoader } from '../ui/LoadingSpinner';

export default Spinner;
export { Spinner, PageLoader, SectionLoader };

